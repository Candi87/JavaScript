/* #################
 * ## Ejercicio 4 ##
 * #################
 *
 * El objeto robot es capaz de desplazarse entre las distintas filas y
 * columnas de un tablero (array multidimensional) tal como:
 *
 *      const board = [
 *                      ['A', 'B', 'C', 'D'],
 *                      ['E', 'F', 'G', 'H'],
 *                      ['I', 'J', 'K', 'L'],
 *                    ];
 *
 *      board[fila][columna]
 *
 *
 * Para tal fin hace uso de cuatro métodos:
 *
 *    - moveUp();
 *
 *    - moveDown();
 *
 *    - moveLeft();
 *
 *    - moveRight();
 *
 *
 * Dichos métodos están preparados para evitar que el robot se salga fuera del array.
 * El robot nos muestra su ubicación actual en el tablero por medio del método
 * "showPosition()";
 *
 * Debemos pasarle un tablero al robot antes de empezar a jugar con él.
 *
 */

'use strict';

// Robot.
const Robot = {
    board: [],
    row: 0,
    column: 0,

    moveUp: function () {
        // ¡Magia!
    },

    moveDown: function () {
        // ¡Magia!
    },

    moveRight: function () {
        // ¡Magia!
    },

    moveLeft: function () {
        // ¡Magia!
    },

    showPosition: function () {
        // ¡Magia!
    },
};

// Tablero.
const myBoard = [
    ['A', 'B', 'C', 'D'],
    ['E', 'F', 'G', 'H'],
    ['I', 'J', 'K', 'L'],
];
